# git的使用
## 新建仓库
```bash
git init [name]
```

创建一个名为`name`的仓库(文件夹)

然后进入仓库可以看见一个`.git`隐藏文件

通过 `ls -Hidden` 查看隐藏文件

`cd .git` + `ls` 查看仓库配置

```bash
git clone [path]
```

从某个路径下克隆一个仓库到本地

如 `git clone https://github.com/Errormecium/Git`


